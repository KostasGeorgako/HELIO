# ── PyTorch (GPU) ─────────────────────────────────────────────────────────────
# Pick ONE --extra-index-url line matching your CUDA version, comment out the rest
--extra-index-url https://download.pytorch.org/whl/cu126   # CUDA 12.6
# --extra-index-url https://download.pytorch.org/whl/cu124 # CUDA 12.4
# --extra-index-url https://download.pytorch.org/whl/cu121 # CUDA 12.1
# --extra-index-url https://download.pytorch.org/whl/cu118 # CUDA 11.8

torch>=2.4.0
torchvision>=0.19.0

# ── Core science stack ────────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.5.0

# ── Hyperspectral / raster I/O ────────────────────────────────────────────────
rasterio>=1.3.10
spectral>=0.23.1          # ENVI .hdr reader if used by spectral_common

# ── Plotting ──────────────────────────────────────────────────────────────────
matplotlib>=3.9.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0              # progress bars if used anywhere